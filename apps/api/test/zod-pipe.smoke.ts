// Superseded by zod-pipe.e2e-spec.ts (ts-jest). Node --strip-types can't compile the pipe's
// TS parameter-property, so the regression test runs under jest instead. This file is a no-op.
export {};
